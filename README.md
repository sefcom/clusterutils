SEFCOM Cluster Utils
====================

Currently one utility is included in this package: `sc-utilization``. This
program will display utilization for the cluster. See `sc-utilization --help`
for more options.
